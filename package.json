{
  "name": "musica",
  "version": "1.0.0",
  "description": "URL to MP3 and MP4 converter",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd frontend && npm install && npm run build",
    "preview": "cd frontend && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  }
}
